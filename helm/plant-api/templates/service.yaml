apiVersion: v1
kind: Service
metadata:
  name: {{ include "plant-api.fullname" . }}
  labels:
    {{- include "plant-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "plant-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP